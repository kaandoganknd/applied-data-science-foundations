# Data files

The source files are not included in the public repository. Before running the syntax, place `Iris_Modified.csv` and `weather.xlsx` in a local folder and update the input paths at the top of each `.sps` file.

The SPSS syntax saves cleaned `.sav` and `.csv` files in this folder. These generated files are not published automatically; they are reviewed before inclusion.
